<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ComicTracking.UserControls.VolumeCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ComicTracking.UserControls"
    xmlns:domain="using:Domain"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="UCLoaded"
    x:Name="UC">

    <UserControl.DataContext>
        <domain:ComicFile/>
    </UserControl.DataContext>

    <Grid ColumnSpacing="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <SymbolIcon Grid.Column="0" Symbol="Stop" Foreground="Gray"/>
        <SymbolIcon x:Name="CFIsFinished" Grid.Column="0" Symbol="Accept" Foreground="Lime" Visibility="{Binding ElementName=UC, Path=ComicFileVisibility}"/>
        <TextBlock x:Name="CFName" Grid.Column="1" Text="{Binding ElementName=UC, Path=ComicFileName}"/>
    </Grid>
</UserControl>
